# Defaults del build de producción (instalador que se descarga): apunta a dev.
# Sin este archivo el build cae a los localhost de src/stores/settings.store.ts.
# Solo URLs — las API keys se ingresan en Configuración → Conexiones.
VITE_GRS_BASE_URL=https://devovrs.srv.win2tec.es/b
VITE_ZEMPO_BASE_URL=https://jud-integration-zempo.srv.win2tec.es/zempo
VITE_ATH_BASE_URL=https://ath-microservice.srv.win2tec.es/api/ath
