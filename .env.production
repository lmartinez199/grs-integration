# Entorno PRODUCCIÓN — cargado por `pnpm tauri build`.
# Ajusta a las URLs reales del despliegue. Sin secretos en el repo.

VITE_GRS_BASE_URL=https://grs.example.com/api
VITE_ZEMPO_BASE_URL=https://zempo.example.com/zempo
VITE_ATH_BASE_URL=https://ath.example.com/api/ath
VITE_LANGUAGE=eng
