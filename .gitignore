# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# TypeScript: cache de build incremental (composite/project references)
*.tsbuildinfo

# Variables de entorno: no se versionan, salvo .env.production (URLs de dev que
# se hornean en el instalador; no lleva secretos).
.env*
!.env.production

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# graphify knowledge graph (generated locally, mantenido por git hooks)
graphify-out/

# Tauri updater: la llave PRIVADA de firma NUNCA se versiona (la pública va en
# tauri.conf.json). Ver docs/auto-update.md.
*.key
