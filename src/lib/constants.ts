/** Intervalo de polling para queries de sync/estado en ms. */
export const SYNC_INTERVAL = 15_000;

/** staleTime por defecto del QueryClient en ms. */
export const QUERY_STALE_TIME = 10_000;

/** Reintentos por defecto del QueryClient. */
export const QUERY_RETRY = 1;
